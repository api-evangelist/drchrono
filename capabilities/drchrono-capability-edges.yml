slug: drchrono
provider: drchrono
generated_by: planning/capability-mapping/scripts/classify_capabilities.py
model: claude-opus-5
frame:
- Healthcare Providers
min_confidence: 0.7
capability_model:
  source: https://github.com/vincentmakes/turbo-ea-capabilities
  license: CC-BY-4.0
  attribution: Turbo EA Capabilities by Vincent Verdet — Turbo EA, https://github.com/vincentmakes/turbo-ea-capabilities,
    CC BY 4.0
  notice: NOTICE
edge_count: 2
edges:
- tag: Billing
  spec_file: drchrono-billing-api-openapi.yml
  capability_id: BC-2870
  capability_id_l1: BC-2870
  capability_name: Healthcare Revenue Cycle Management
  confidence: 0.9
  evidence: 'GET /api/eligibility_checks; GET /api/patient_authorizations; GET /api/day_sheet_patient_payments;
    schemas: BillingLineItem, LineItemTransaction, CustomInsurancePlanName, Coverage, BillingProfile'
  reason: Operations cover charge line items, insurance plans/coverage, eligibility checks, authorizations,
    day-sheet charges/credits and patient payments — provider-side revenue cycle. Spans several sub-capabilities
    (charge capture, eligibility, cash posting), so mapped at L1.
- tag: Clinical
  spec_file: drchrono-clinical-api-openapi.yml
  capability_id: BC-2830
  capability_id_l1: BC-2830
  capability_name: Clinical Documentation Management
  confidence: 0.78
  evidence: 'POST /api/clinical_notes_list  clinical_notes_list_create; PUT /api/lab_orders/{id}  lab_orders_update;
    schemas: ClinicalNote, SoapNoteLineItemFieldType, PatientVaccineRecord, SignedConsentForm'
  reason: Surface is dominated by clinician documentation artefacts — clinical notes, SOAP note field
    types, lab orders, consent forms, vitals and vaccine records — i.e. authoring and order entry in the
    active patient record. Breadth across notes, orders and results keeps this at L1.
