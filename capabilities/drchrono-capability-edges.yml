slug: drchrono
provider: drchrono
generated_by: planning/capability-mapping/scripts/classify_capabilities.py
model: claude-opus-5
frame:
- Healthcare Providers
min_confidence: 0.7
capability_model:
  source: https://github.com/vincentmakes/turbo-ea-capabilities
  license: CC-BY-4.0
  attribution: Turbo EA Capabilities by Vincent Verdet — Turbo EA, https://github.com/vincentmakes/turbo-ea-capabilities,
    CC BY 4.0
  notice: NOTICE
edge_count: 2
edges:
- tag: Billing
  spec_file: drchrono-billing-api-openapi.yml
  capability_id: BC-2870
  capability_id_l1: BC-2870
  capability_name: Healthcare Revenue Cycle Management
  confidence: 0.9
  evidence: GET /api/eligibility_checks, GET /api/patient_authorizations, POST /api/line_items_list, GET
    /api/day_sheet_patient_payments; schemas BillingLineItem, Coverage, CashPayment, LineItemTransaction
  reason: Provider-side billing surface spanning charge line items, payer eligibility/authorisation and
    patient payments — squarely healthcare revenue cycle. Spans several sub-capabilities (charge capture,
    eligibility, cash posting), so L1 only.
- tag: Clinical
  spec_file: drchrono-clinical-api-openapi.yml
  capability_id: BC-2830
  capability_id_l1: BC-2830
  capability_name: Clinical Documentation Management
  confidence: 0.82
  evidence: POST /api/clinical_notes_list, PUT /api/lab_orders/{id}, POST /api/signed_consent_forms; schemas
    ClinicalNote, LabTest, PatientVaccineRecord, SoapNoteLineItemFieldType
  reason: Operations author and maintain the active clinical record — clinical notes, orders, consent
    forms, vitals/vaccine records — which is clinical documentation management. Breadth spans notes, order
    entry and results, so L1 only.
